name: Deploy Portfolio
on:
  push:
    branches: [ "main" ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy
        id: deploy
        uses: actions/deploy-pages@v4
        
      - name: Ping search engines
        continue-on-error: true
        run: |
          echo "Pinging search engines about sitemap update..."
          # Wait a bit for deployment to propagate
          sleep 5
          # Google
          curl -f "http://www.google.com/ping?sitemap=https://bibhu342.github.io/bibhuai/sitemap.xml" || echo "Google ping failed (non-blocking)"
          # Bing
          curl -f "http://www.bing.com/ping?sitemap=https://bibhu342.github.io/bibhuai/sitemap.xml" || echo "Bing ping failed (non-blocking)"
          echo "Search engine pinging completed"


